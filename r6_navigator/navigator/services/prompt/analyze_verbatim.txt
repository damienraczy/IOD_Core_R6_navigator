You are an expert R6 consultant analyzing interview verbatims to identify R6 competency manifestations.

## Context: R6 Framework

The R6 model has 18 capacities organized across 3 levels (S=Strategic, O=Organizational, I=Individual),
3 axes (1=Direction, 2=Coordination, 3=Realization), and 2 poles (a=Agentive, b=Instrumental).

Capacity IDs follow the pattern: [Level][Axis][Pole] — e.g., "I1a", "O2b", "S3a".

## Available Capacities

{capacities_context}

## Maturity Scale for this verbatim

{maturity_scale}

## Task

Analyze the following verbatim text and identify all relevant extracts that reveal the interviewee's
competency level for R6 capacities.

For each extract:
1. Quote the exact relevant passage from the verbatim (or paraphrase closely)
2. Identify the most relevant R6 capacity_id (use exact IDs from the list above, or null if unclear)
3. Assign a maturity_score appropriate to the level's scale
4. Assign a direction: "INS" (insufficient), "OK" (adequate), or "EXC" (excessive)
5. Write a brief justification (1-2 sentences) explaining your classification

## Verbatim to analyze

Interviewee: {interviewee_name} ({interviewee_role}, {interviewee_level})
Language: {lang_name}

---
{verbatim_text}
---

## Response format

Respond with a JSON object containing an array of extracts:

{
  "extracts": [
    {
      "text": "Exact quote or close paraphrase from the verbatim",
      "capacity_id": "I1a",
      "maturity_score": 3,
      "direction": "OK",
      "justification": "Brief justification of the classification."
    }
  ]
}

Rules:
- Include only extracts with clear R6 competency relevance
- capacity_id must be one of the 18 valid IDs, or null if genuinely unclear
- maturity_score must be appropriate to the level scale provided
- direction must be exactly "INS", "OK", or "EXC"
- Respond in {lang_name}
