Generate professional R6 content for the following capacity.
Respond in {lang_name}.

Capacity: {capacity_id} — {canonical_name}
Level: {level_name} ({level_code})
Axis: {axis_name} ({axis_number})
Pole: {pole_name} ({pole_code})

=== MANDATORY HALLIDAY LINGUISTIC RULES FOR LEVEL {level_code} ===
{halliday_context}

Apply the rules above to every sentence you generate. Never omit the subject. The subject must be the participant type required for level {level_code} as described above. Never use ellipsis.

=== FIELD SPECIFICATIONS ===

name: The canonical label of this capacity. Use exactly: {canonical_name}

definition: 5 sentences describing what this capacity IS and what it encompasses.
- Each sentence MUST use the level-appropriate explicit subject (Acteur, Porteur/Acteur abstrait, or Sensant/Identifié — as required for level {level_code})
- Each sentence MUST use the correct Halliday process type for level {level_code}
- Each sentence must be less than 20 words
- Format: bullet list `- Sentence.\n`

central_function: 3 continuous sentences (not a bullet list) describing the strategic role and purpose of this capacity for executives and top managers.
- Apply Halliday rules for level {level_code}
- Do not use ellipsis; include the explicit subject in each sentence

observable: 5 sentences describing the concrete, typical manifestations of this capacity when it is present and operational at the NORMAL expected level — neither deficient nor excessive.
- Each sentence MUST use the level-appropriate explicit subject
- Each sentence MUST use the correct Halliday process type for level {level_code}
- Each sentence must be less than 20 words
- Format: bullet list `- Sentence.\n`

risk_insufficient: 5 sentences describing observable signs of DEFICIT — the capacity is absent, underdeveloped, or chronically avoided, causing measurable dysfunction.
- Each sentence MUST use the level-appropriate explicit subject
- Each sentence MUST use the correct Halliday process type for level {level_code}
- Each sentence must be less than 20 words
- Format: bullet list `- Sentence.\n`

risk_excessive: 5 sentences describing observable signs of PATHOLOGICAL OVERUSE — the capacity is overactivated, rigid, compulsive, or deployed so far beyond its useful range that it creates dysfunction (over-control, rigidity, systemic imbalance). This is NOT about high performance or surpassing expectations; it describes a dysfunctional extreme.
- Each sentence MUST use the level-appropriate explicit subject
- Each sentence MUST use the correct Halliday process type for level {level_code}
- Each sentence must be less than 20 words
- Format: bullet list `- Sentence.\n`

Return a JSON object with exactly these keys:
  name, definition, central_function, observable, risk_insufficient, risk_excessive
