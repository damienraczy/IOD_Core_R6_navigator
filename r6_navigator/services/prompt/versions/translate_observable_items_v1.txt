Translate the following R6 observable behavioral manifestations from {source_lang_name} to {target_lang_name}.

Capacity ID: {capacity_id} — Level: {level_name} ({level_code})

Return a JSON object with these keys: OK, DEP, EXC, INS
(only include keys present in the source).
Each key maps to a list of translated strings (same order and count as source).

Each string must comply with Halliday's transitivity rules for the level ({level_code}), remain less than 20 words, and use ellipsis of the subject.

Source items:
{source_items}
